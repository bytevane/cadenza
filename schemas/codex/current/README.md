# Codex schema artifacts

Run `./scripts/codex-schema.sh` after pinning a Codex CLI/app-server version.

Generated files in this directory must match the exact Codex version used by production images.
